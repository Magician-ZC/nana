# Nana项目 - 完整交接文档

> 交接日期：2025年10月21日  
> 项目名称：Nana - 二次元AI伴侣  
> 交接人：[您的姓名]  
> 接收人：[待填写]

---

## 📋 目录

1. [项目概述](#1-项目概述)
2. [技术栈](#2-技术栈)
3. [系统架构](#3-系统架构)
4. [环境配置](#4-环境配置)
5. [核心模块说明](#5-核心模块说明)
6. [部署说明](#6-部署说明)
7. [常见问题及解决方案](#7-常见问题及解决方案)
8. [未完成事项](#8-未完成事项)
9. [重要联系方式](#9-重要联系方式)
10. [附录](#10-附录)

---

## 1. 项目概述

### 1.1 项目简介
Nana是一个基于大语言模型的二次元AI伴侣应用，主要特点：
- 💬 智能对话系统，支持多轮上下文对话
- 🎤 语音交互功能（语音识别 + 语音合成）
- 🎨 Live2D角色展示，支持多种角色模型
- 🧠 长期记忆系统，可记住用户偏好和信息
- 🤖 自定义Agent系统，用户可创建个性化AI角色
- 📊 心理评估功能

### 1.2 项目目标
为用户提供一个能够进行深度情感交流、具有记忆能力的AI伴侣，支持文字和语音多模态交互。

### 1.3 当前状态
- ✅ 核心功能已完成
- ✅ 前后端基本功能稳定运行
- ⚠️ 部分高级功能需要优化（详见第8章）
- 📝 文档完整度：中等

---

## 2. 技术栈

### 2.1 后端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.10.14 | 后端主语言 |
| FastAPI | 0.115.12 | Web框架 |
| OpenAI SDK | 1.68.2 | LLM接口调用 |
| ChromaDB | 1.0.9 | 向量数据库（记忆存储） |
| Uvicorn | 0.22.0 | ASGI服务器 |
| Pydantic | 2.10.6 | 数据验证 |
| aiohttp | 3.11.16 | 异步HTTP客户端 |
| websockets | 15.0.1 | WebSocket支持 |

**主要Python依赖**：
```
fastapi==0.115.12
openai==1.68.2
chromadb==1.0.9
uvicorn==0.22.0
aiohttp==3.11.16
websockets==15.0.1
pydantic==2.10.6
```

### 2.2 前端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | 22.12.0 | 前端运行环境 |
| Vue.js | 3.4.19 | 前端框架 |
| Vite | 6.0.5 | 构建工具 |
| Pinia | 2.1.7 | 状态管理 |
| Vue Router | 4.2.5 | 路由管理 |
| Pixi.js | 6.0.0 | Live2D渲染 |
| pixi-live2d-display | 0.4.0 | Live2D展示 |
| TailwindCSS | 3.3.5 | CSS框架 |

---

## 3. 系统架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────┐
│                   前端 (Vue.js)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ ChatPanel│  │Live2D Model│ │Settings │          │
│  └──────────┘  └──────────┘  └──────────┘          │
│          HTTP/WebSocket 通信                         │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│              后端 API (FastAPI)                      │
│  ┌──────────────┐  ┌──────────────┐                │
│  │ chat_service │  │ main_agent   │                │
│  └──────────────┘  └──────────────┘                │
│         ↓                  ↓                         │
│  ┌──────────────┐  ┌──────────────┐                │
│  │ llm_service  │  │ tts_service  │                │
│  └──────────────┘  └──────────────┘                │
└─────────────────────────────────────────────────────┘
         ↓                  ↓                ↓
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   LLM API    │  │   ChromaDB   │  │ 科大讯飞TTS  │
│  (本地/云端) │  │  (向量存储)  │  │  Fish Audio  │
└──────────────┘  └──────────────┘  └──────────────┘
```

### 3.2 核心数据流

**对话流程**：
1. 用户输入 → 前端ChatPanel
2. 发送到后端 `/chat` API
3. ChatService 接收请求
4. MainAgent 处理用户意图
5. LLMService 调用大模型生成回复
6. 可选：TTSService 生成语音
7. 流式返回给前端
8. 前端展示文字 + 播放语音

**记忆系统流程**：
1. 对话过程中提取用户信息
2. UserInfoProcessor 处理用户信息
3. 存储到 ChromaDB 向量库
4. 每次对话时检索相关记忆
5. 注入到对话上下文

---

## 4. 环境配置

### 4.1 开发环境要求
- **操作系统**: Windows/Linux/MacOS
- **Node.js**: v22.12.0 (或其他兼容版本)
- **Python**: 3.10.14 (推荐使用虚拟环境)
- **内存**: 建议 8GB+ (本地运行大模型需要更多)
- **GPU**: 可选 (本地运行大模型时需要)

### 4.2 关键配置文件

#### 后端配置 (`backend/config.py`)

```python
# LLM配置
LLM_API_URL = "http://本地IP:11434"  # 本地Ollama或其他OpenAI兼容API
LLM_API_KEY = ""  # API密钥
LLM_MODEL = "qwen2.5:32b"  # 模型名称

# 向量模型配置
EMBEDDING_API_KEY = "sk-..."  # SiliconFlow API Key
EMBEDDING_API_URL = "https://api.siliconflow.cn/v1/embeddings"
EMBEDDING_MODEL = "BAAI/bge-m3"
EMBEDDING_DIMENSION = 1024

# 科大讯飞语音识别配置
XFYUN_APP_ID = "..."
XFYUN_API_KEY = "..."
XFYUN_API_SECRET = "..."

# TTS配置
ENABLE_TTS_GLOBAL = False  # 全局TTS开关
ENABLE_TTS = False  # 普通TTS
ENABLE_SUPER_TTS = False  # 超拟人TTS
```

**⚠️ 重要提醒**：
- config.py 中包含敏感信息（API密钥），不要提交到公开仓库
- 建议使用环境变量管理敏感配置
- 现有的API密钥需要妥善保管和交接

#### 前端配置 (`frontend/vite.config.js`)

```javascript
// 主要配置后端API地址
server: {
  proxy: {
    '/api': 'http://localhost:8000'
  }
}
```

### 4.3 第三方服务账号

| 服务 | 用途 | 账号信息 | 备注 |
|------|------|----------|------|
| SiliconFlow | 向量模型API | ⚠️需交接 | 用于embedding |
| 科大讯飞 | 语音识别+TTS | APP_ID见config.py | 需要实名认证 |
| Fish Audio | TTS服务 | ⚠️需交接 | 目前未配置 |
| 七牛云 | 文件存储 | ⚠️需交接 | qiniu_uploader.py |

**⚠️ 交接清单**：
- [ ] SiliconFlow API密钥
- [ ] 科大讯飞开发者账号
- [ ] 七牛云存储账号（如使用）
- [ ] LLM服务商账号（如使用云端API）

---

## 5. 核心模块说明

### 5.1 后端核心模块

#### 5.1.1 主入口 (`main.py`)

**文件路径**: `backend/main.py`

**主要功能**：
- FastAPI应用初始化
- CORS配置
- 路由注册
- WebSocket连接管理

**关键API端点**：
```python
POST /chat              # 普通对话
POST /stream_chat       # 流式对话
GET  /conversations     # 获取对话列表
POST /switch_agent      # 切换Agent
GET  /user_info         # 获取用户信息
POST /upload_audio      # 上传音频
...（约30+个端点）
```

**注意事项**：
- 文件较大（约37905 tokens），建议分模块重构
- HTTPS证书自动生成逻辑在 `generate_cert.py`

#### 5.1.2 对话服务 (`chat_service.py`)

**核心类**: `ChatService`

**主要职责**：
- 管理对话流程
- 协调各个子服务（LLM、TTS、Agent）
- 处理用户输入和生成回复
- 管理对话历史

**关键方法**：
```python
async def chat(user_input, user_id, agent_id, ...)  # 普通对话
async def stream_chat(...)  # 流式对话
async def chat_with_vision(...)  # 多模态对话
async def guided_chat(...)  # 引导式对话
```

**特殊功能**：
- 引导模式对话（用于心理评估）
- 自定义Agent支持
- 多用户管理

#### 5.1.3 LLM服务 (`llm_service.py`)

**核心类**: `LLMService`

**主要功能**：
- 调用OpenAI兼容的LLM API
- 流式生成支持
- Token计数和管理

**支持的模型**：
- 本地模型（通过Ollama/LM Studio）
- 云端模型（OpenAI、Claude等）

#### 5.1.4 主Agent (`main_agent.py`)

**核心类**: `MainAgent`

**功能**：
- 加载和管理Agent提示词
- 意图识别和提取
- 用户信息管理
- 对话上下文构建

**Agent类型**：
- 内置Agent: nanaA, nanaB, nanaC
- 自定义Agent: 用户创建的个性化角色
- 外部Agent: 从市场导入

**提示词位置**: `backend/prompts/`

#### 5.1.5 记忆系统 (`embedding.py`, `user_info_processor.py`)

**向量数据库**: ChromaDB

**存储内容**：
- 对话历史（长期记忆）
- 用户个人信息（姓名、喜好、性格等）
- 重要对话片段

**文件位置**：
- 向量数据库: `backend/save/memory/chroma.sqlite3`
- 用户信息文本: `backend/save/{user_id}/me.txt`

#### 5.1.6 语音服务

**TTS服务**：
- `tts.py`: 科大讯飞普通TTS
- `super_tts.py`: 科大讯飞超拟人TTS
- 支持多种音色选择

**语音识别**：
- `speech_service.py`: 科大讯飞语音识别
- `streaming_sensevoice_service.py`: 流式语音识别

**音频文件存储**: `backend/save/custom_voice/`

#### 5.1.7 评估系统 (`assessment_api.py`)

**功能**：
- 心理评估问卷
- 视频分析评估
- 生成PDF报告

**依赖**：
- PDF生成: ReportLab
- 图像处理: OpenCV, Pillow
- OCR: Tesseract

**输出位置**:
- PDF报告: `backend/save/assessments/`
- 视频评估: `backend/save/video_assessments/`

### 5.2 前端核心模块

#### 5.2.1 路由结构 (`src/router/`)

```
/ (Home)              # 主界面
├── /login            # 登录页（未使用）
└── /register         # 注册页（未使用）
```

#### 5.2.2 状态管理 (`src/stores/`)

**Store列表**：
- `chatStore.js`: 聊天状态管理
- `userStore.js`: 用户状态管理
- `settingsStore.js`: 设置状态管理

#### 5.2.3 核心组件

| 组件 | 路径 | 功能 |
|------|------|------|
| ChatPanel.vue | components/ | 聊天面板主组件 |
| Live2DModel.vue | components/ | Live2D模型展示 |
| AgentSelector.vue | components/ | Agent选择器 |
| AgentMarket.vue | components/ | Agent市场 |
| Settings.vue | components/ | 设置面板 |
| VideoRecorder.vue | components/ | 视频录制（评估用） |
| SensevoiceRecorder.vue | components/ | 语音录制 |

**Live2D模型位置**: `frontend/public/models/`

**可用模型**：
- Haru (春) - 默认角色
- Chitose, Epsilon, Hibiki, Hiyori, Izumi, Kei, Mao...
- 共约19个模型

**⚠️ 重要提醒**：
- Live2D模型是从工坊购买的，仅供学习交流
- 禁止用于商业用途
- 模型文件较大，部署时注意

---

## 6. 部署说明

### 6.1 开发环境部署

#### 后端部署

```bash
# 1. 进入后端目录
cd backend

# 2. 创建虚拟环境
python -m venv venv

# 3. 激活虚拟环境
# Windows:
.\venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 4. 安装依赖
pip install -r requirements.txt

# 5. 配置config.py
# 编辑backend/config.py，填入必要的API密钥

# 6. 启动服务
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# 访问: http://localhost:8000
# API文档: http://localhost:8000/docs
```

#### 前端部署

```bash
# 1. 进入前端目录
cd frontend

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 访问: http://localhost:5173
```

### 6.2 生产环境部署（建议）

#### 后端部署

**使用Supervisor管理进程**：
```ini
[program:nana-backend]
directory=/path/to/nana-main/backend
command=/path/to/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
user=www-data
autostart=true
autorestart=true
stderr_logfile=/var/log/nana/backend.err.log
stdout_logfile=/var/log/nana/backend.out.log
```

**使用Docker**（推荐）：
```dockerfile
# 后端Dockerfile示例
FROM python:3.10.14-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

#### 前端部署

```bash
# 1. 构建生产版本
npm run build

# 2. 输出目录: dist/
# 3. 使用Nginx部署
```

**Nginx配置示例**：
```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /path/to/nana-main/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端API代理
    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # WebSocket支持
    location /ws {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

### 6.3 HTTPS配置

**开发环境**：
- 后端会自动生成自签名证书
- 位置: `backend/ssl/`
- 脚本: `generate_cert.py`

**生产环境**：
- 建议使用Let's Encrypt免费证书
- 或购买商业SSL证书

---

## 7. 常见问题及解决方案

### 7.1 后端问题

#### Q1: LLM连接失败
**错误**: `Connection refused` 或 `API key invalid`

**解决方案**：
1. 检查 `config.py` 中的 `LLM_API_URL` 是否正确
2. 如使用本地模型，确保Ollama/LM Studio已启动
3. 检查API密钥是否有效
4. 查看日志: `backend/save/log/YYYYMMDD.txt`

#### Q2: ChromaDB初始化错误
**错误**: `chromadb.errors.InvalidDimensionException`

**解决方案**：
1. 删除 `backend/save/memory/chroma.sqlite3`
2. 重启服务，会自动重建
3. 确保 `EMBEDDING_DIMENSION` 配置正确

#### Q3: TTS服务不可用
**错误**: 语音无法生成

**解决方案**：
1. 检查科大讯飞账号是否欠费
2. 验证 `XFYUN_APP_ID`, `XFYUN_API_KEY`, `XFYUN_API_SECRET`
3. 临时禁用TTS: 设置 `ENABLE_TTS_GLOBAL = False`

#### Q4: 内存占用过高
**现象**: Python进程占用大量内存

**解决方案**：
1. 减少 `MAX_TURNS` (对话历史轮数)
2. 定期清理 `backend/save/log/`
3. 重启服务释放内存

### 7.2 前端问题

#### Q1: Live2D模型加载失败
**错误**: 模型不显示或报错

**解决方案**：
1. 检查 `public/models/` 目录完整性
2. 检查浏览器控制台是否有CORS错误
3. 确保模型路径配置正确

#### Q2: WebSocket连接失败
**错误**: 无法接收流式消息

**解决方案**：
1. 检查后端是否正常运行
2. 查看浏览器控制台WebSocket状态
3. 检查防火墙/代理设置

#### Q3: 音频播放问题
**错误**: TTS音频无法播放

**解决方案**：
1. 检查浏览器是否允许自动播放音频
2. 用户需要先与页面交互（点击按钮）
3. 检查音频文件格式支持

### 7.3 部署问题

#### Q1: 端口占用
**错误**: `Address already in use`

**解决方案**：
```bash
# Windows
netstat -ano | findstr :8000
taskkill /PID <PID> /F

# Linux
lsof -i :8000
kill -9 <PID>
```

#### Q2: 跨域问题
**错误**: CORS policy error

**解决方案**：
1. 检查 `main.py` 中的CORS配置
2. 确保允许前端域名访问
3. 开发环境可以使用 `allow_origins=["*"]`

---

## 8. 未完成事项

### 8.1 高优先级（建议1-2周内完成）

- [ ] **代码重构**: `main.py` 文件过大，建议拆分为多个模块
- [ ] **错误处理**: 完善异常捕获和用户友好提示
- [ ] **日志系统**: 统一日志格式，添加日志级别控制
- [ ] **测试用例**: 缺少单元测试和集成测试
- [ ] **性能优化**: 
  - 对话历史数据库查询优化
  - 向量检索性能优化
  - 前端组件懒加载

### 8.2 中优先级（建议1个月内完成）

- [ ] **用户系统**: 
  - 登录/注册功能（前端已有组件但未启用）
  - 用户权限管理
  - 多用户隔离
- [ ] **Agent市场**: 
  - Agent分享功能完善
  - Agent评分和评论
  - 热门Agent推荐
- [ ] **数据备份**: 
  - 自动备份用户数据
  - 对话历史导出功能
- [ ] **监控告警**: 
  - API调用量监控
  - 错误率告警
  - 资源使用监控

### 8.3 低优先级（可选功能）

- [ ] **多模态支持**: 
  - 图片理解（已有部分代码）
  - 视频分析优化
- [ ] **国际化**: 支持多语言
- [ ] **移动端适配**: 响应式优化
- [ ] **主题系统**: 更多UI主题选项
- [ ] **插件系统**: 支持第三方扩展

### 8.4 已知Bug

1. **内存泄漏**: 长时间运行后内存占用增加（定期重启可缓解）
2. **WebSocket断线重连**: 偶尔需要刷新页面
3. **TTS队列**: 快速发送多条消息时TTS可能错乱
4. **Live2D性能**: 低端设备上可能卡顿

---

## 9. 重要联系方式

### 9.1 项目相关

| 角色 | 联系方式 | 备注 |
|------|----------|------|
| 项目创建者 | B站: [3546572358945017](https://space.bilibili.com/3546572358945017) | 原作者 |
| 前任负责人 | ⚠️待填写 | - |

### 9.2 技术支持

| 服务 | 联系方式 | 文档 |
|------|----------|------|
| 科大讯飞 | https://www.xfyun.cn/ | 开放平台文档 |
| SiliconFlow | https://siliconflow.cn/ | API文档 |
| Fish Audio | https://fish.audio/zh-CN/ | TTS文档 |
| Ollama | https://ollama.com/ | 本地LLM部署 |

### 9.3 社区资源

- Vue.js官方文档: https://vuejs.org/
- FastAPI官方文档: https://fastapi.tiangolo.com/
- Live2D文档: https://www.live2d.com/
- ChromaDB文档: https://docs.trychroma.com/

---

## 10. 附录

### 10.1 目录结构详解

```
nana-main/
├── backend/                    # 后端代码
│   ├── prompts/               # Agent提示词
│   │   ├── nanaA.txt          # 默认角色A
│   │   ├── nanaB.txt          # 默认角色B
│   │   └── custom/            # 自定义提示词
│   ├── save/                  # 数据存储目录
│   │   ├── conversations/     # 对话历史
│   │   ├── memory/            # 向量数据库
│   │   ├── custom_agents/     # 自定义Agent
│   │   ├── users/             # 用户数据
│   │   ├── log/               # 日志文件
│   │   └── assessments/       # 评估报告
│   ├── main.py                # 主入口文件
│   ├── config.py              # 配置文件 ⚠️
│   ├── chat_service.py        # 对话服务
│   ├── llm_service.py         # LLM服务
│   ├── main_agent.py          # Agent管理
│   ├── embedding.py           # 向量嵌入
│   ├── tts.py                 # TTS服务
│   ├── requirements.txt       # Python依赖
│   └── venv/                  # 虚拟环境（不提交）
├── frontend/                  # 前端代码
│   ├── src/
│   │   ├── components/        # Vue组件
│   │   │   ├── ChatPanel.vue  # 聊天面板
│   │   │   ├── Live2DModel.vue# Live2D展示
│   │   │   ├── AgentMarket.vue# Agent市场
│   │   │   └── Settings.vue   # 设置
│   │   ├── views/             # 页面视图
│   │   ├── stores/            # Pinia状态管理
│   │   ├── router/            # 路由配置
│   │   └── App.vue            # 根组件
│   ├── public/
│   │   ├── models/            # Live2D模型 ⚠️
│   │   └── avatars/           # 头像图片
│   ├── package.json           # 前端依赖
│   ├── vite.config.js         # Vite配置
│   └── node_modules/          # 依赖包（不提交）
├── README.md                  # 项目说明
└── LICENSE                    # 开源协议
```

### 10.2 数据备份建议

**需要定期备份的目录**：
1. `backend/save/` - 所有用户数据和对话历史
2. `backend/save/memory/` - 向量数据库
3. `backend/save/custom_agents/` - 自定义Agent
4. `backend/prompts/custom/` - 自定义提示词

**备份命令**：
```bash
# Windows
xcopy /E /I /Y backend\save D:\backup\nana\save_YYYYMMDD

# Linux/Mac
cp -r backend/save /backup/nana/save_$(date +%Y%m%d)
```

**建议备份频率**：
- 每日备份: 对话数据
- 每周备份: 全量数据
- 每月归档: 长期存储

### 10.3 性能监控指标

**建议监控的指标**：
1. **响应时间**: API平均响应时间 < 2s
2. **LLM调用**: Token使用量和成本
3. **错误率**: API错误率 < 1%
4. **并发用户**: 同时在线用户数
5. **内存使用**: Python进程内存 < 2GB
6. **数据库大小**: ChromaDB文件大小

### 10.4 推荐的开发工具

**后端开发**：
- IDE: PyCharm / VS Code
- API测试: Postman / Thunder Client
- 数据库查看: SQLite Browser

**前端开发**：
- IDE: VS Code
- 浏览器: Chrome DevTools
- Vue调试: Vue DevTools

**Git工具**：
- GUI: SourceTree / GitKraken
- 命令行: Git Bash

### 10.5 环境变量配置（推荐）

为了安全，建议使用环境变量管理敏感配置：

**创建 `.env` 文件** (不要提交到Git):
```bash
# LLM配置
LLM_API_URL=http://localhost:11434
LLM_API_KEY=sk-xxx
LLM_MODEL=qwen2.5:32b

# 向量模型
EMBEDDING_API_KEY=sk-xxx
EMBEDDING_API_URL=https://api.siliconflow.cn/v1/embeddings

# 科大讯飞
XFYUN_APP_ID=xxx
XFYUN_API_KEY=xxx
XFYUN_API_SECRET=xxx
```

**加载环境变量**:
```python
from dotenv import load_dotenv
load_dotenv()

LLM_API_KEY = os.getenv("LLM_API_KEY")
```

### 10.6 Git提交规范

**建议使用语义化提交**:
```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 代码重构
perf: 性能优化
test: 测试相关
chore: 构建/工具链
```

**示例**:
```bash
git commit -m "feat: 添加自定义Agent导出功能"
git commit -m "fix: 修复TTS队列错乱问题"
git commit -m "docs: 更新部署文档"
```

---

## 📝 交接确认清单

请在交接时确认以下事项：

### 代码交接
- [ ] 代码仓库访问权限已转移
- [ ] 所有分支已同步到远程仓库
- [ ] 未提交的本地修改已处理

### 环境交接
- [ ] 开发环境配置文档已提供
- [ ] 所有依赖包版本已记录
- [ ] API密钥和凭证已安全转交

### 数据交接
- [ ] 用户数据已备份
- [ ] 数据库文件已交接
- [ ] 重要日志文件已归档

### 账号交接
- [ ] 科大讯飞账号已转移
- [ ] SiliconFlow账号已转移
- [ ] 七牛云账号已转移（如有）
- [ ] 域名管理账号已转移（如有）

### 文档交接
- [ ] 本交接文档已审阅
- [ ] API文档已更新
- [ ] 部署文档已验证

### 知识传递
- [ ] 已进行面对面交接会议
- [ ] 核心功能已演示
- [ ] 常见问题已说明
- [ ] 紧急联系方式已提供

---

## 🎯 给接手人员的建议

1. **先运行起来**: 按照部署说明把项目跑起来，熟悉基本功能
2. **阅读代码**: 从 `main.py` → `chat_service.py` → `main_agent.py` 的顺序阅读
3. **测试功能**: 逐一测试所有API端点，理解数据流
4. **查看日志**: 查看 `backend/save/log/` 了解系统运行情况
5. **小步迭代**: 先修复小bug，再做大重构
6. **文档优先**: 遇到问题先查文档，再看代码
7. **保持备份**: 修改前一定要备份数据

**祝你顺利！** 🚀

---

**文档版本**: v1.0  
**最后更新**: 2025年10月21日  
**交接人**: [您的姓名]  
**文档状态**: ✅ 完成

---

## 附加资源

### 推荐阅读
- [FastAPI官方教程](https://fastapi.tiangolo.com/tutorial/)
- [Vue3组合式API](https://vuejs.org/guide/introduction.html)
- [ChromaDB使用指南](https://docs.trychroma.com/)
- [LM Studio使用文档](https://lmstudio.ai/docs)

### 视频教程
- B站搜索: "FastAPI教程"
- B站搜索: "Vue3实战"
- B站搜索: "本地部署大模型"

### 问题反馈
如果在交接过程中发现本文档有遗漏或错误，请及时反馈更新。

---

**感谢你的付出，祝未来一切顺利！** 💐

